# REQUIRED — Neon Postgres (use sslmode=verify-full to silence warnings)
DATABASE_URL=postgresql://user:pass@host/db?sslmode=verify-full

# REQUIRED — must match the Lovable app secrets
GATEWAY_API_KEY=change-me
WEBHOOK_SECRET=change-me
WEBHOOK_URL=https://smart-biz-responder.lovable.app/api/wa/incoming

PORT=3000
